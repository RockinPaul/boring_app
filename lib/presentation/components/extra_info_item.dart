import 'package:flutter/material.dart';

class ExtraInfoItem extends StatelessWidget {
  const ExtraInfoItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
